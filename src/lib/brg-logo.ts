import brgLogoAsset from "../assets/brg-logo.png.asset.json";

/** Local public copy — same 7763-byte asset as the Lovable upload; works in dev & production. */
export const BRG_LOGO_SRC = "/brg-logo.png";

/** Lovable CDN path when deployed on their platform (kept for reference). */
export const BRG_LOGO_LOVABLE_SRC = brgLogoAsset.url;
